import {
} from "../ActionTypes";

const initialCityState = {
};

const city = (state = initialCityState, action) => {
  switch (action.type) {
   
    default:
      return state;
  }
};

export default city;
