import {
} from "../ActionTypes";

const initialCountryState = {
};

const country = (state = initialCountryState, action) => {
  switch (action.type) {
   
    default:
      return state;
  }
};

export default country;